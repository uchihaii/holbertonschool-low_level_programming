#include "main.h"

/**
 *  * mul - multiply the integers
 *   * @a: first integer to multiply
 *    * @b: second integer to multiply
 *     * Return: returns the c variable
 *      */

int mul(int a, int b)
{
	int c;
	c = a * b;
	return (c);
}
